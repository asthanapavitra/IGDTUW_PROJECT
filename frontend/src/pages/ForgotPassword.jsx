import React, { useState, useEffect, useRef } from "react";
import { gsap } from "gsap";

const ForgotPassword = () => {
//   const [step, setStep] = useState(1);
//   const emailRef = useRef(null);
//   const questionRef = useRef(null);
//   const passwordRef = useRef(null);

//   useEffect(() => {
//     if (step === 2) {
//       gsap.to(emailRef.current, { translateX: "100%", opacity: 0, duration: 0.5 });
//       gsap.fromTo(questionRef.current, { translateX: "0", opacity: 0 }, { x: "0%", opacity: 1, duration: 0.5 });
//     } else if (step === 3) {
//       gsap.to(questionRef.current, { x: "100%", opacity: 0, duration: 0.5 });
//       gsap.fromTo(passwordRef.current, { x: "-100%", opacity: 0 }, { x: "0%", opacity: 1, duration: 0.5 });
//     }
//   }, [step]);

  return (
    <h1>hello</h1>
  );
};

export default ForgotPassword;
